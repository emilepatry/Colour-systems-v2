Run the Heron AI Illustration Pipeline on the provided Visual Treatment Variables JSON to produce a production-ready Midjourney V7 prompt.

## Steps

1. **Read the pipeline spec and knowledge files.** Before processing the JSON, read all four of these files in full:

   - `illustration generator/knowledge/cursor-prompt-agent-01-02-chain (1).md` — the combined Agent 01→02 system prompt. Follow the system prompt defined inside this file exactly.
   - `illustration generator/knowledge/agent-01-visual-treatment-director.md` — Agent 01 decision logic, the 20 Visual Treatment Variables spec, approval tier matrix, and failure modes.
   - `illustration generator/knowledge/agent-02-prompt-architect-midjourney (1).md` — Agent 02 prompt construction rules, parameter reference, structured output schema, and iteration strategy.
   - `illustration generator/knowledge/brand knowledge base/START-HERE.md` — Brand Knowledge Base index. Read this first, then read all files in the `brand knowledge base/` directory for Midjourney V7 syntax, parameter reference, negative prompt assembly rules, style reference strategy, visual quality benchmarks, and prompt templates.

2. **Treat the user's pasted content as the pipeline input.** The user will paste a JSON object alongside this command. This JSON was generated by the Illustration Prompt Builder UI and contains the 20 core Visual Treatment Variables plus optional enrichment fields (`flow_context`, `celebration_level`, `secondary_pillars`, `secondary_journey_moments`).

3. **Run Phase 1 (Visual Treatment Director).** Analyze the variables and determine whether this screen needs an illustration, what type, the concept direction, approval tier, and flags. If no illustration is needed, output the recommendation and rationale and stop.

4. **Run Phase 2 (Prompt Architect).** If Phase 1 recommends an illustration, construct the complete Midjourney prompt following the rules in the combined pipeline spec — prompt structure order, parameter selection, negative prompt assembly, and background handling.

5. **Output in the exact format specified in the combined pipeline spec:**

   - **Reasoning** — Recommendation, Concept, Approval tier, Key rationale, Flags
   - **Midjourney Prompt** — Single copy-paste-ready string with all `--parameters`
   - **Notes for Designer** — Compliance routing, draft mode suggestions, `--sref` reminders, and any context-specific guidance

## Rules

- Do not guess at brand rules, parameter defaults, or negative prompt terms — use the specs from the knowledge files.
- Do not skip reading the knowledge files. The pipeline depends on the decision logic, prompt construction rules, and Midjourney syntax documented in them.
- Do not modify the output format. Designers depend on the consistent Reasoning / Prompt / Notes structure.
- If the JSON is missing required fields or contains conflicting values, note the issue in the Reasoning section and proceed with your best interpretation.
